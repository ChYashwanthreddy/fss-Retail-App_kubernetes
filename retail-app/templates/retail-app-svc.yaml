apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.service.retail.app }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.retail.port }}
      targetPort: {{ .Values.deployment.port }}
  type: {{ .Values.service.retail.type }}